{
  "project": "ran-feed",
  "description": "内容/信息流后台系统 — Go 微服务项目",
  "last_updated": "2026-06-16",
  "_meta_note": "重构起点 从零跟踪功能状态",
  "_field_guide": {
    "id": "feat-XXX 新功能 / fix-XXX 修复 / sec-XXX 安全 / opt-XXX 优化 / refactor-XXX 重构",
    "name": "简短名称",
    "description": "做什么",
    "dependencies": ["前置条目 id"],
    "status": "not-started | in-progress | done",
    "evidence": "完成证据 验证如何通过"
  },
  "features": [
    {
      "id": "refactor-001",
      "name": "count canal 消费者重构与策略重抽象",
      "description": "Consume 拆成解析/路由/去重/落库/派发五步管道 消息标识与累积器与副作用各自成文件 落库分支收口 applyUpdate；strategy 按判活谓词加目标映射重抽象为 presence 与 reset 两族并拆子包 删除 followCountTableStrategy 等重复；新增服务级 RecordStatus 枚举 复用 pkg/enum.IsDeleted；补单测",
      "dependencies": [],
      "status": "done",
      "evidence": "./init.sh 通过 build 加 vet 加 test 全绿；新增单测覆盖 presence 各表 INSERT/DELETE/UPDATE 翻转 reset 级联清零 去重幂等 ParseInt64 RecordStatus；纯结构重构对外行为不变"
    },
    {
      "id": "refactor-002",
      "name": "收藏链路去 Lua 化",
      "description": "interaction 侧：删除 favorite:rel 关系缓存与负缓存及 BuildFavoriteRelKey 删 add_user_favorite_if_exists.lua；QueryFavoriteInfo 改直接回源 DB(favoriteRepo.IsFavorited) 去掉关系缓存与分布式锁重建；Favorite/RemoveFavorite 改纯 cache-aside 仅删用户收藏 feed 头部缓存。content 侧：user_favorite_feed L1 改热头部旁路缓存 只缓存最新 capacity=300 条(TTL=24h) 页落头部内走 ZrevrangebyscoreWithScoresAndLimit 缓存 翻过头部回 DB QueryFavoriteList 单页；ensureHotHead 用 Zadds+Zremrangebyrank+Expire 去 Lua 重建；删 QueryUserFavoriteZSetScript 及 query_user_favorite_zset.lua 删分布式锁/轮询/listAllFavorites/in-memory 分页/keepN=5000；接入 capacity/expire 常量",
      "dependencies": [],
      "status": "done",
      "evidence": "./init.sh 通过 build vet test 全绿；收藏流 L1 全去 Lua 改 go-zero 单命令 热头部 300 条旁路缓存 越界回 DB 个人流无并发同 key 故不加锁；删 content 侧 query_user_favorite_zset.lua 与 Go 变量无残留引用"
    },
    {
      "id": "refactor-003",
      "name": "feed 二级缓存内容详情统一",
      "description": "为 content 服务所有 feed 流(recommend/follow/user_publish/user_favorite)统一二级缓存内容详情。L1 排序 ZSET 各 feed 自管不动；新增 L2 按 content_id 缓存内容本征详情(content_type/author_id/title/cover_url/published_at/visibility)与观察者无关 author 名头像走 user 服务 usercache like 易变读时旁挂不入缓存。存储 String 加 JSON 加 MGET key content:detail:{id} 与 usercache 同构；统一入口 contentcache.BatchGet(ids loader) miss==过期单分支 批量回源 TTL 叠 jitter 负哨兵防穿透 只降级不阻断 不做击穿防护(L2 重建廉价 分布式锁留给 L1)。四条 feed 的 buildBriefMaps 回表拼装收口到共享 contentDetailResolver；recommend/follow 读时按 visibility 过滤 PUBLIC follow 因返回 FollowFeedItem 用 resolveDetails 加 buildFollowItems 自组装；delete_content 写路径接 contentcache.Invalidate",
      "dependencies": [],
      "status": "done",
      "evidence": "./init.sh 通过 build vet test 全绿；新增 contentcache 包单测覆盖全命中/部分命中/去重跳 id<=0/miss 回填正值与 TTL/负哨兵写入与命中跳 loader/关缓存直回源/loader 错误透传/Invalidate 删 key；四条 feed 各删一份重复 buildBriefMaps+buildUserAndLikeMaps+buildItems 收口共享 resolver；纯重构对外行为不变 publish/favorite 不过滤可见性 recommend/follow 保持 PUBLIC 过滤"
    },
    {
      "id": "refactor-004",
      "name": "关注流重构为基于 deadline 的时间窗口推拉流",
      "description": "把关注流从'按条数截断的永久收件箱'重构为'最近 T=14 天时间窗口缓存'(T 可配 FollowFanOut.DeadlineWindowDays 默认 14)。①大 V 模型：viewer 侧逐个 count-rpc 扫描(≤500次/5min)+500 截断漏读 改为全局集合 feed:bigv:global——count 的 CanalCountConsumer 在 FOLLOWED 计数变更后按阈值 SADD/SREM 增量维护(effects.go reconcileBigVSet)，count 新增 BigVRebuildJob(internal/job)启动预热+每小时全量重建(临时 key 批量 SADD 后 RENAME 原子切换)兜底漂移与 Redis 丢失；content 写扩散 fanout 与读路径 bigv_merge 改 SISMEMBER/SMEMBERS∩关注(去 500 截断 cap 提 5000)。②deadline 地基：inbox/publish zset score 由 content_id 改 published_at 毫秒；写 lua 加 ZREMRANGEBYSCORE 窗口裁剪+EXPIRE 续期；query lua 游标改时间戳+min=cutoff+WITHSCORES 返回(member,score)；新增 followwindow helper(CutoffMillis/TTLSeconds/WriteArgs)与 lua_reply.parseZSetReply/scoredID/mergeScored 按 published_at 归并；冷路径 ListFollowByAuthorsCursor 与 user_publish 全改 published_at 游标。inbox 窗口裁剪 publish 不裁剪(承载 user_publish_feed 全量历史 窗口只读时施加)。③关注 backfill：FollowUser 加翻转判定只在 unfollow→follow 触发；content BackfillFollowInbox 失效 viewer bigv 缓存+大 V 跳过+窗口对齐(publish zset 冷则回源 DB ListPublishedByAuthorWithinWindow 只取 PUBLIC)。④取关清理：新增 RPC PurgeFolloweeFromInbox(改 proto goctl 生成)失效 bigv 缓存+大 V 跳过+ZREM followee 窗口内 content_id；UnfollowUser 翻转判定+异步调用。⑤大 key：inbox 窗口裁剪+keepN+整 key TTL 冷用户自动回收。⑥收尾：listFollowees 三合一 listFolloweesCapped；backfill/purge/fanout 取数与大 V 判定去重收口 follow_inbox_source(loadFolloweeWindowContent/isBigVAuthor)。Part B 业务标记(认证号)需加 schema 字段 后置未做。",
      "dependencies": ["refactor-001", "refactor-003"],
      "status": "done",
      "evidence": "./init.sh 通过 build vet test 全绿(content/count/interaction 三服务+proto goctl 重新生成)；大 V 判定读写统一全局集合 CDC 增量+周期重建双保障 消除 N 次 count-rpc 与 500 截断漏读；inbox/publish 全切 published_at 时间游标 inbox 带窗口裁剪与 TTL publish 仅 keepN+TTL 保留全量历史；关注 backfill/取关 purge 与写扩散推拉边界对称(大 V 一律跳过 推/回填/清理 由读时 merge 覆盖)；取关主动 ZREM 清理脏数据+follow/unfollow 均失效 viewer bigv 缓存；listFollowees 与窗口取数/大 V 判定去重无残留。项目未上线无存量迁移负担。"
    },
    {
      "id": "refactor-005",
      "name": "关注流推拉模型加固 大V sticky 落库 读路径统一 复合游标 查询去 Lua",
      "description": "对照 docs/follow-feed-design.md 共识方案在 refactor-004 基础上加固关注流。①大 V 改 sticky 永不降级：新增 count 服务 ran_feed_big_v 表(只增不删 真相源)+BigVRepository；CDC effects.go syncBigVMember 跨阈值只 INSERT IGNORE 落表+SADD 去掉 SREM 降级分支 消除降级空档与阈值抖动；阈值提到 50000。②重建任务由 time.Ticker 自循环改 xxl-job 触发定时修正 BigVReconcileJob(internal/cron/bigv_reconcile)：先复查 count_value≥阈值补 CDC 漏网晋升(ListTargetValuesByValueGte) 再以大 V 表为真相源 RENAME 重灌全局集合兜底 Redis 丢失；count.go 接 xxljob executor+XxlJob 配置 删旧 internal/job。③inbox 口径统一只装小号：重建/冷兜底用 loadGlobalBigVSet 过滤大 V(filterSmallFollowees) 大 V 永远读时 merge 不进 inbox。④读路径冷热统一：FollowFeed 结果=merge(inbox 来源,大V publish) 大 V merge 提到分支外永远执行 消除只关注大V用户首屏空白；异步重建+coldBackfill 合并为同步 buildInboxSync(一次查询既填缓存又出首屏 删 goroutine/bg ctx/重建锁)；空结果写不可见哨兵(id<=0)负缓存 杜绝空用户反复扫库。⑤复合游标 score:id：mergeScored/pageScoredFromRows 按(score,id)双键过滤排序出游标 修同毫秒翻页 skip/重复 兼容旧纯 millis 游标。⑥查询去 Lua(读不用 Lua 原则)：inbox 与大 V publish 查询退成原生 ZrevrangebyscoreWithScoresByFloatAndLimit+Exists+Expire 删 query_follow_inbox_zset.lua；backfill lua 删末尾 N 次 ZSCORE 改累加 ZADD 返回值；update lua 因 rebuild 批量大 unpack 有栈溢出风险保留循环。⑦升级 go-zero v1.10.2 用其 DoCtx 通用命令口 大 V 修正 RENAME 由 Eval 改原生 DoCtx。作者主页流 user_publish_feed 仍用 QueryUserPublishZSetScript 待后续统一。",
      "dependencies": ["refactor-004"],
      "status": "done",
      "evidence": "./init.sh 通过 build vet test 全绿；大 V 改 sticky+DB 表真相源 CDC 只晋升 xxl-job 定时修正(补漏+RENAME 重灌)兜底 Redis 丢失；inbox 只装小号 读路径冷热统一永远 merge 大 V 消除首屏空白 同步构建合并双查询删锁；空哨兵负缓存挡住空用户重扫；复合游标 score:id 修同分翻页 inbox/publish 查询原生化删一支 query lua backfill 去 ZSCORE 循环；go-zero v1.10.2 RENAME 改 DoCtx。作者主页流统一留作后续。"
    },
    {
      "id": "refactor-006",
      "name": "发件箱查询抽象 publishbox 大V读路径切换 分布式锁防击穿",
      "description": "把 publish zset(发件箱 feed:user:publish:{uid})的查询从读路径里抽出来 新建 internal/logic/publishbox 包统一封装。①核心方法 PublishBox.QueryWindow(authorID,cutoffMillis,cursorScore,pageSize)：命中直接读 未命中回源重建 空作者写哨兵防穿透。读法沿用复合游标语义——裸 ZrevrangebyscoreWithScoresByFloatAndLimit maxScore inclusive 边界同分交 mergeScored 按 id 过滤(不能套用 own-feed 的 exclusive 游标 query lua 否则漏边界项)；空结果再 EXISTS 区分 cache miss 与窗口内真空。②防击穿用项目公共 cache.DoWithLock(DistLocker)：大V发件箱是跨 pod 热点 全集群只放一个重建+拿锁后双检+未拿锁轮询等待复用 取代手搓 RedisLock+time.Sleep；svcCtx 挂 PublishBoxRebuildLocker=cache.NewDistLocker(复用 redisClient) 对齐 interaction 的 LikeUserRebuildLocker 范式；ErrLockBusy 本轮该作者降级缺席不阻塞整流。③重建用 context.WithoutCancel 脱离请求取消 回源走 ListPublishedByAuthorWithinWindow(sinceMillis=0 PUBLIC-only 与 fanout 写扩散口径一致) 写满或写哨兵后回读本页。④bigv_merge_helper 的 fetchBigVContentIDs 切到 box.QueryWindow 删除裸读的 queryBigVPublishIDs——修掉大V发件箱 TTL 过期后内容从所有关注者 feed 永久消失的假空当真空 bug。写侧(fanout/发布)与作者主页流 own-feed 本次不动。",
      "dependencies": ["refactor-005"],
      "status": "done",
      "evidence": "./init.sh 通过 build vet test 全绿；新建 publishbox 包封装发件箱命中读/未命中重建/空哨兵；防击穿改用公共 cache.DoWithLock(DistLocker) 跨 pod 单飞重建 对齐 LikeUserRebuildLocker；回源 PUBLIC-only 重建用脱离请求 ctx；bigV merge 切 QueryWindow 删裸读 修复发件箱过期内容永久消失的假空 bug；ErrLockBusy 降级该作者缺席。"
    },
    {
      "id": "fix-006",
      "name": "作者主页流 own-feed 迁移 publishbox 修复跨可见性泄露",
      "description": "[Phase 2 待办] own-feed(UserPublishFeed)的 loadPageIDs 冷重建走 ListPublishedByAuthor 不带 Visibility.Eq(PUBLIC) 过滤 会把 PRIVATE 内容灌进与大V merge 共享的同一个 feed:user:publish:{uid} key；bigV merge 读同 key 喂给关注者 → 作者私密内容可能泄露给关注者(取决于该发件箱最后由哪条路径冷重建)。修法：把 own-feed 的 loadPageIDs 迁到 publishbox.QueryWindow(cutoffMillis=0 取全量历史) 复用 PUBLIC-only 重建口径 一并消除泄露 并删掉 user_publish_feed_logic 里重复的锁+重建+in-memory 分页代码。注意 own-feed 现用 exclusive 字符串游标 迁移后改复合游标语义需回归翻页行为。",
      "dependencies": ["refactor-006"],
      "status": "not-started",
      "evidence": ""
    }
  ]
}
